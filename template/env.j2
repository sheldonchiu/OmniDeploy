current_dir=$(dirname "$(realpath "${BASH_SOURCE[0]}")")
source $WORKING_DIR/.env
source $WORKING_DIR/utils/helper.sh

{% if export_required_env %}
# This is only used by entry.sh to check if the required envs are set
{{ export_required_env }}
{% else %}
export REQUIRED_ENV=""
{% endif %}

{{ setup_env }}
